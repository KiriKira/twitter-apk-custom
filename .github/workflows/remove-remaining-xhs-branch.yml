name: Remove Remaining XHS Branch

on:
  push:
    branches:
      - main
    paths:
      - ".github/workflows/remove-remaining-xhs-branch.yml"

permissions:
  contents: write

jobs:
  cleanup:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - name: Delete remaining XHS branch
        shell: bash
        run: git push origin --delete xhs-fold-patch
